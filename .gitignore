# .gitignore

# Node dependencies
node_modules/

# Netlify specific
.netlify/
netlify.toml # If you want to keep this local only, but often committed
             # However, for this project, it's fine to commit as it has no secrets.

# macOS specific
.DS_Store

# Optional: If you ever generate local config files for other experiments
# env-config.js
# config.js

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*